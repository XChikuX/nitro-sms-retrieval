///
/// HybridSMSRetrieverSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

// Forward declaration of `SMSStatus` to properly resolve imports.
namespace margelo::nitro::huymobile::smsretriever { struct SMSStatus; }
// Forward declaration of `SMSError` to properly resolve imports.
namespace margelo::nitro::huymobile::smsretriever { struct SMSError; }

#include <string>
#include <NitroModules/Promise.hpp>
#include <optional>
#include "SMSStatus.hpp"
#include <functional>
#include "SMSError.hpp"

namespace margelo::nitro::huymobile::smsretriever {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `SMSRetriever`
   * Inherit this class to create instances of `HybridSMSRetrieverSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridSMSRetriever: public HybridSMSRetrieverSpec {
   * public:
   *   HybridSMSRetriever(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridSMSRetrieverSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridSMSRetrieverSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridSMSRetrieverSpec() override = default;

    public:
      // Properties
      virtual bool getIsListening() = 0;
      virtual bool getIsRegistered() = 0;

    public:
      // Methods
      virtual void startListening() = 0;
      virtual std::shared_ptr<Promise<std::string>> startListeningWithPromise(std::optional<double> timeoutMs) = 0;
      virtual void stopListening() = 0;
      virtual std::shared_ptr<Promise<std::string>> getAppHash() = 0;
      virtual std::shared_ptr<Promise<SMSStatus>> getStatus() = 0;
      virtual std::function<void()> onSMSRetrieved(const std::function<void(const std::string& /* otp */)>& callback) = 0;
      virtual std::function<void()> onSMSReceived(const std::function<void(const std::string& /* message */)>& callback) = 0;
      virtual std::function<void()> onSMSError(const std::function<void(const SMSError& /* error */)>& callback) = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "SMSRetriever";
  };

} // namespace margelo::nitro::huymobile::smsretriever
